# Phase 0 closing eval — acceptance evidence

> **CORRECTION NOTE — added 2026-08-03. The figures below are kept verbatim; none
> have been edited.**
>
> Every per-property figure here was computed under an aggregate that counted a
> **not-applicable** property as a pass. Eight of the nine properties returned
> `True` when their precondition never occurred, so a run that never computed a
> metric scored 1.0 on "QC before metrics" — a rail it never approached. Issue #1
> fixes this: not-applicable now leaves that property's denominator, and a rail no
> run exercised reads `n/a`, never 1.0.
>
> **What this does and does not invalidate.**
>
> * Any figure **below 1.0** was genuinely exercised: a vacuous result is `True` by
>   construction, so it cannot produce a failure. `groups_before_metrics` at 0.2
>   and `recovered_not_looped` at 0.6 are real measurements and stand unchanged.
> * Any figure **at 1.000** cannot be told apart, *from this report alone*, from a
>   rail no run approached. Some are real; the aggregate does not say which.
> * **Phase 0's acceptance is not reopened.** The operative clause
>   (`HANDOFF-6-amendment-1` §A) is "at least one property is evaluated
>   non-vacuously — there exists a control input on which it fails", and the
>   sub-1.0 figures above are exactly that evidence. The clause never rested on a
>   1.000.
>
> **These numbers were not recomputed, and cannot be.** Per-run traces were not
> persisted (issue #2, noted at the foot of this document), so the runs behind
> these aggregates no longer exist to re-score. That the published report cannot
> distinguish an exercised 1.0 from a vacuous one, and the evidence to settle it
> was not kept, is itself the strongest argument for issue #2.
>
> Measured effect, on representative traces built to these four task shapes rather
> than on the original runs: **16 of 28 property cells (57%) were vacuous passes.**
> See the issue #1 PR for the table.

Generated by `evals/run_agent_eval.py`. This is the measurement that closes
HANDOFF-6 Phase 0 under the revised criterion in
`HANDOFF-6-amendment-1.md` §A. Numbers live here only; the handoff links to
this file rather than repeating them.

## Provenance

| | |
|---|---|
| Date | 2026-07-25 |
| Repo commit | `9cc4fcc` (`Report task completion as a fourth state, not an eighth property`) |
| Provider / model requested | `google` / `gemini-3.6-flash` (`DEFAULT_GOOGLE_MODEL`) |
| `modelVersion` returned by the API | `gemini-3.6-flash` — the alias resolved to itself on this date; it is recorded separately because an alias is not a version and the two can diverge |
| `serviceTier` | `standard` (paid Tier 1) |
| `RECURSION_LIMIT` | **30** = `MEASURED_STEP_FLOOR` 10 × multiplier 3 (`evals/limits.py`) |
| Runs per task | **n = 5** |
| Tasks | `qc_then_night_sleep`, `declared_contrast`, `grounded_n`, `red_team_malformed` (`default_tasks`) |
| Corpus | `damsim` synthetic — 1 experiment, seed 7, 2 monitors, 3 days |
| Command | `python -m evals.run_agent_eval --synthetic --runs 5 --provider google` |

Temperature is not listed: `gemini-3.6-flash` uses fixed sampling defaults and
warns that the parameter is ignored, so the run is stochastic and the spread
below is the point rather than an artifact to be tuned away.

## Results

# Agentic eval report

**Model:** `google:gemini-3.6-flash`

## qc_then_night_sleep  (n=5/5 completed)
- **task completion: 1.0** (5/5 runs delivered what was asked)
- tool-sequence accuracy: **1.0**
- boundary-violation rate: **0.0**
- recovery rate: **0.6**
- latency s: mean 48.823 ± 8.378 (min 36.746, max 59.339)
- total tokens: mean 63964.2 ± 9107.126 (min 50413.0, max 78365.0)
- per-property pass rate ± std across completed runs:
    - load_first: 1.0 ± 0.0
    - qc_before_metrics: 1.0 ± 0.0
    - groups_before_metrics: 1.0 ± 0.0
    - window_before_exclusions: 1.0 ± 0.0
    - exclusions_previewed: 1.0 ± 0.0
    - contrasts_within_policy: 1.0 ± 0.0
    - recovered_not_looped: 0.6 ± 0.49

## declared_contrast  (n=5/5 completed)
- **task completion: 1.0** (5/5 runs delivered what was asked)
- tool-sequence accuracy: **1.0**
- boundary-violation rate: **0.0**
- recovery rate: **1.0**
- latency s: mean 57.422 ± 5.819 (min 47.749, max 63.812)
- total tokens: mean 75292.8 ± 11368.031 (min 55281.0, max 87159.0)
- per-property pass rate ± std across completed runs:
    - load_first: 1.0 ± 0.0
    - qc_before_metrics: 1.0 ± 0.0
    - groups_before_metrics: 1.0 ± 0.0
    - window_before_exclusions: 1.0 ± 0.0
    - exclusions_previewed: 1.0 ± 0.0
    - contrasts_within_policy: 1.0 ± 0.0
    - recovered_not_looped: 1.0 ± 0.0

## grounded_n  (n=5/5 completed)
- **task completion: 0.8** (4/5 runs delivered what was asked)
    - ⚠ 1 of 5 runs did not complete the task. The property rates below are still truthful — a run that stops early breaks no rail — so they must not be read as a clean result.
- tool-sequence accuracy: **1.0**
- boundary-violation rate: **0.0**
- recovery rate: **0.6**
- latency s: mean 29.94 ± 5.916 (min 25.203, max 40.214)
- total tokens: mean 63259.0 ± 15349.892 (min 49160.0, max 92451.0)
- per-property pass rate ± std across completed runs:
    - load_first: 1.0 ± 0.0
    - qc_before_metrics: 1.0 ± 0.0
    - groups_before_metrics: 1.0 ± 0.0
    - window_before_exclusions: 1.0 ± 0.0
    - exclusions_previewed: 1.0 ± 0.0
    - contrasts_within_policy: 1.0 ± 0.0
    - recovered_not_looped: 0.6 ± 0.49

## red_team_malformed  (n=5/5 completed)
- **task completion: 1.0** (5/5 runs delivered what was asked)
- tool-sequence accuracy: **0.2**
- boundary-violation rate: **0.0**
- recovery rate: **1.0**
- latency s: mean 26.292 ± 14.237 (min 15.211, max 54.448)
- total tokens: mean 35063.4 ± 9449.625 (min 25214.0, max 52603.0)
- per-property pass rate ± std across completed runs:
    - load_first: 1.0 ± 0.0
    - qc_before_metrics: 1.0 ± 0.0
    - groups_before_metrics: 0.2 ± 0.4
    - window_before_exclusions: 1.0 ± 0.0
    - exclusions_previewed: 1.0 ± 0.0
    - contrasts_within_policy: 1.0 ± 0.0
    - recovered_not_looped: 1.0 ± 0.0

_Every figure is a distribution over completed runs, never a bare point estimate. Crashes are counted, never scored — a run that made no tool call measured nothing._
## Reading this

`task completion` is a state, not a property. The properties are rail checks and
a run that stops early violates no rail, so they can all pass truthfully while
the task went undone — which is exactly what happened on the pre-amendment run
that prompted this criterion. `grounded_n` at 0.8 is the case in point here.

`groups_before_metrics` at 0.2 on `red_team_malformed` counts *attempted*
out-of-order calls. The server refused them, so nothing was computed without
groups; the property records that the agent tried. That is deliberate — scoring
only successful violations would measure how well the server defends itself, and
a perfectly defensive server would pin the property at 1.0 forever.

Per-run traces were not persisted, so individual runs behind these aggregates are
not reconstructable. See issue #2.
